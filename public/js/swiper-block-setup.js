addEventListener('DOMContentLoaded', () => {
    const swiper = new Swiper('[data-name=swiper-block]', {
        loop: true,
        pagination: {
            el: '.swiper-pagination',
        },
    });
})

